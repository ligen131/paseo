# Inline thinking timeline example

This plugin replaces reasoning rows from the first streaming delta and renders the paced text
inline. Install the directory as `inline-thinking`, then run an agent that streams reasoning.
